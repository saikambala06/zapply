# Gemini build configuration

The application uses the Google Gemini REST API directly and does not require an additional AI SDK dependency. The only AI environment variables are `GOOGLE_API_KEY` and the optional `GEMINI_MODEL`.
